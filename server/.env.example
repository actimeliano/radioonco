PORT=3000

# SMTP configuration (optional). If omitted, messages are stored in data/messages.jsonl
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_user@example.com
SMTP_PASS=your_password

# Contact email routing
CONTACT_TO=owner@example.com
CONTACT_FROM=website@example.com
